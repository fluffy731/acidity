name: acidic
services:
  app:
    image: acidic:local
    build:
      context: .
      target: runner
    restart: unless-stopped
    init: true
    environment:
      ACIDIC_MODE: preview
    ports:
      - "3100:3000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
